import "package:flutter/material.dart";
import "./geocoding.container.dart";

class GeocodingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: GeocodingContainer(),
    );
  }
}
