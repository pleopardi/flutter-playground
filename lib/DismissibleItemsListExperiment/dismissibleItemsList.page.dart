import "package:flutter/material.dart";

class DismissibleItemsListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Dismissible Items List Page"),
      ),
    );
  }
}
